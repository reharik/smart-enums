# Changelog

## 0.1.5

- Internal cleanup only — no behavior or API change: the enum-field resolution helper returns `undefined` instead of `null` for non-enum types, and an `interface` became a `type` alias. (0.1.4 was version-alignment only and never published; npm goes straight from 0.1.3 to 0.1.5.)

## 0.1.3 and earlier

See git history.
